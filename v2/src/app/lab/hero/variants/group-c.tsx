"use client";

// PROTOTYPE — throwaway. Background variants, group C: motion graphics (SVG, CSS, Canvas 2D).

import { liveryRibbons } from "./livery-ribbons";
import { trackGrid } from "./track-grid";
import { speedStreaks } from "./speed-streaks";
import { scrollShift } from "./scroll-shift";
import { dustMotes } from "./dust-motes";
import type { Variant } from "./types";

export const groupC: Variant[] = [liveryRibbons, trackGrid, speedStreaks, scrollShift, dustMotes];
